{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "기본 권한 — 모든 네이티브 동작은 Rust 에서 처리하므로 원격 웹 콘텐츠에는 IPC 권한을 주지 않는다.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
