# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env (anon key는 공개해도 되지만 .env는 커밋하지 않음)
.env
.env.*
!.env.example

# Cloudflare 로컬 시크릿 / 캐시
.dev.vars
.wrangler

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 참고 자료(대용량 PDF)는 커밋 제외
*.pdf

# 데스크톱 업데이터 서명 개인키 — 절대 커밋 금지(서명은 CI 의 secret 으로 수행)
src-tauri/.updater-private-key

# 순위수집 스파이크/핸드오프(파서·계약·service_role 키 포함) — 커밋 금지, 로컬 참고용
spike-rank/
