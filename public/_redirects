# 존재하는 정적 파일은 Cloudflare Pages가 먼저 서빙. 아래는 "없는 경로"에만 적용된다.
#  ★ 해시 자산(/assets/*)이 없으면(배포로 해시 바뀐 옛 청크 요청 등) index.html(HTML)을 주지 말고 404로.
#    안 그러면 브라우저가 .js 대신 HTML을 받아 "Unexpected token '<'" → 백지가 된다.
/assets/* 404
# 그 외 모든 경로는 SPA 진입점으로(클라이언트 라우팅).
/* /index.html 200
